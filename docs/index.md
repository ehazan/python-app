# Documents for python-app

This application has two endpoints:
- `localhost/api/v1/info`
- `localhost/api/v1/healthz`

# How to access the app ?

You can each the app by accessing this URL
localhost/api/v1/healthz